box: ruby
build:
    steps:
        - script:
            name: echo
            code: |
                echo "hello world!"
    after-steps:
        - wantedly/pretty-slack-notify:
            webhook_url: $SLACK_WEBHOOK_URL
            channel: status
